const defaultState = {
    events: []
};

const EventReducer = (state = defaultState, {type, payload}) => {

    switch (type) {

    }

    return state;

};

export default EventReducer;
